Finance & Billing scaffold — developer guide.

- Implement interactive billing forms in `template.html` and logic in `index.js`.
- Use `/api/finance` for seed data; extend server to add more endpoints if needed.
- Use `State` for role checks and permissions.
