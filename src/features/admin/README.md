Admin Users UI — developer scaffold

- Navigate to `/admin` after signing in as an `admin` role.
- Allows viewing, editing (name/email/role), and deleting users persisted in `src/data/users.json`.
- For local dev only: passwords are stored as SHA-256 hashes in `users.json` when created via `/api/register`.
