Admin feature

- `template.html` -> admin UI
- `styles.css` -> admin styles
- `index.js` -> exports `init(mount, State)`; requires admin role

APIs used:
- GET `/api/users`
- PUT `/api/users/:id`
- DELETE `/api/users/:id`
