Operations feature scaffold.

How to implement:
- Edit `template.html` to add controls.
- Update `index.js` to fetch `/api/operations` and render.
- Use `State` to access current user and permissions.

Run: start the static server (`npm start`) and open `/operations` route.
